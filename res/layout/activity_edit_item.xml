<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".EditItemActivity" >

    <TextView
        android:id="@+id/tvEditTextLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/edit_item_label" />

    <EditText
        android:id="@+id/etEditListItemValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvEditTextLabel"
        android:layout_below="@+id/tvEditTextLabel"
        android:layout_marginTop="21dp"
        android:ems="10"
        android:inputType="textMultiLine" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/editSaveButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/etEditListItemValue"
        android:layout_below="@+id/etEditListItemValue"
        android:layout_marginTop="15dp"
        android:onClick="saveEdit"
        android:text="@string/edit_save_button" />

    <Button
        android:id="@+id/gotoListButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/editSaveButton"
        android:layout_alignBottom="@+id/editSaveButton"
        android:layout_alignRight="@+id/etEditListItemValue"
        android:text="@string/goto_list_button" />

</RelativeLayout>